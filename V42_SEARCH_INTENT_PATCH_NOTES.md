# Silver Today V42 · Search Intent Landing Patch

기준: V41 SEO Intent Patch + 사용자 제공 키워드마스터 스크린샷(2026-08-28).

## 핵심 방향
- 메인 최우선: `은 시세`
- 제2 랜딩: `은 1kg` / `은 1kg 시세` / `은 1kg 가격`
- 보조 랜딩: `은 1돈`, `은 ETF`, `은 시세 계산기`
- `실버바 가격`/`은 투자`는 내부 체류와 다음 질문 연결 역할을 강화

## 주요 변경
1. 메인 title/H1/description을 `오늘 은 시세` 중심으로 재정렬
2. 모바일 첫 화면 바로가기: 1kg / 1돈 / 계산기 / 실버바 / 차트 / ETF
3. 메인 1kg 카드 우선 강조 및 100g·500g 계산기 딥링크
4. `많이 찾는 은 시세 바로가기` 추가
5. 기존 history를 재사용하는 1개월·3개월·1년 은 시세 그래프 추가
6. 1kg 페이지를 국제 환산가 / KB 살 때 / KB 팔 때 / 살→팔 가격차 구조로 강화
7. 1돈 페이지에 `팔 때 가격` 검색 의도 설명 추가
8. 계산기 1g / 1돈 / 100g / 500g / 1kg 원터치 프리셋 + `?grams=` 딥링크
9. ETF 페이지에 국내/미국/롤오버/비용·세금 빠른 탐색
10. 메인·1kg·1돈 구조화 데이터(JSON-LD) 보강
11. sitemap 핵심 페이지 lastmod 반영

## 안정성
- `app-v31.js` 수정 없음
- `home-v40.js` 수정 없음
- Cloudflare Functions 수정 없음
- 외부 API 추가 없음
- `home-v42.js`는 이미 로드한 `state.hist`만 재사용
- 기존 필수 DOM ID 유지

## 키워드 데이터 주의
키워드마스터 수치는 Google 공식 Keyword Planner 통계와 동일하다고 단정하지 않고, 상대적 우선순위와 검색 의도 참고값으로만 사용했습니다.
